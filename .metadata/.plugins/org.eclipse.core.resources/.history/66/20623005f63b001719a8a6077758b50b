package finalJava;

import javax.swing.*;
public class FlipACoin {

	

	public static void displayOutcome()
	{
		
		 final int MIN = 0;
		    final int MAX = 1;
		    int random;
		    int guess;
		    boolean isMatch;
		    int plays = 5;
		    int thisManyTimes = 0;
		   
		    String guessString;
		    
		    JOptionPane.showMessageDialog(null, MoneyTotal.getMoneyTotal());
		    for(int i = 0; i < plays; i++)
		    {
		    random = MIN + (int)(Math.random() * MAX);
		    guessString = JOptionPane.showInputDialog(null, "Guess " + MIN + " for Heads and " +
		    MAX + " for Tails.", JOptionPane.INFORMATION_MESSAGE);
		    guess = Integer.parseInt(guessString);
		    isMatch = guess == random;
		    JOptionPane.showMessageDialog(null, "The number was " + random + ". The result is " + isMatch + ".");
		    ++thisManyTimes;
		    JOptionPane.showMessageDialog(null, "You have tried " + thisManyTimes + " you have a total of " + plays + " left."); 
		    }
	}
}
